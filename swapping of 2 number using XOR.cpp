#include<bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    cout << "Before swapping: a = " << a << ", b = " << b << endl;

    // Swapping using XOR
 y
    cout << "After swapping: a = " << a << ", b = " << b << endl;

    return 0;
}